// import * as cdk from 'cfn-cdk-lib';
// import { Template } from 'cfn-cdk-lib/assertions';
// import * as MultiTenant from '../lib/multi-tenant-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/multi-tenant-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new MultiTenant.MultiTenantStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
